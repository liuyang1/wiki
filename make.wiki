== debug param ==
* -n            --just-print (but shell cmd will run)
* -p            --print-database
* --warn-undefined-variables


=== --debug ===
basic, verbose, implicit, jobs, all, makefile

== shell ==
$$SHELL_VAR_NAME

default using shell is /bin/sh, change this by SHELL var.

cannot run enviroment function from parent shell, but can source and run it in subshell
